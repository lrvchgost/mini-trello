# История шагов

Точка возобновления: последняя запись со статусом `done`. Следующий шаг — первый `todo`
с закрытыми зависимостями. Шаблон и правила — [plan/README.md](plan/README.md).

| Дата       | Шаг  | Что сделано                                                                 | Как проверить                         | Commit | Статус |
|------------|------|------------------------------------------------------------------------------|---------------------------------------|--------|--------|
| 2026-09-21 | 0.0  | Архитектурная ревизия: ADR-008 (ownership-only), снят BullMQ/@nestjs/axios, seed 20/доску, `isDone`, `404` для чужой доски, `column.*`/`clientId`, CI (redis+images), nginx SSE, dedup по `clientId` | grep-чеклист + `git diff docs/` | db5b5ff | done   |
| 2026-09-21 | 0.0a | Корректирующий проход: X-Client-Id для REST-мутаций, actorId/clientId во всех WS-payload, кросс-модульная проводка column.*, CI VITE_*, compose без env_file, фикс таблицы | grep + `git diff docs/` | —      | done   |
| 2026-09-21 | 0.0b | Фиксы ревью: complete DI-мок, exports/эмиссия BoardsGateway, resolver по `:id`, owner-only assignee, CI unit/e2e, nginx healthcheck, repos для search/dashboard/activity, isDone в API, dev-compose в README | grep + `git diff docs/` | —      | done   |
| 2026-09-21 | 0.0c | Фикс resolver: `resolveBoardId(req)` (метод + шаблон маршрута), синхронизирован план 2.1 | grep + `git diff docs/` | —      | done   |
| 2026-09-21 | 0.0d | Критичные фиксы: `log(boardId, action, payload)` + `req.board`, сортировка карточек в `findById`, `test:e2e` в CI | grep + `git diff docs/` | —      | done   |
| 2026-09-21 | 0.0e | Критичные фиксы плана: register без `upsert` (`409`), `forwardRef` Boards⇄Activity, `CORS_ORIGIN` в compose, single-flight refresh + grace, refresh токена для SSE/WS, Redis Pub/Sub для activity, `DATABASE_URL_TEST` в CI, `${VAR:-default}` в compose, раздельные tsconfig FE/BE | grep-чеклист + `git diff docs/` | —      | done   |
| 2026-09-21 | 0.0f | Правки по итогам ревью: глобальные `RepositoriesModule`/`BoardAccessModule` (снят `forwardRef`), `bcrypt`→`bcryptjs`, Alpine+openssl+`binaryTargets`, `navigator.locks`/`BroadcastChannel` + `REFRESH_GRACE_SECONDS`, атомарный `409`, Redis-dedup SSE, retry `Column.order`, zod v3 | grep-чеклист + `git diff docs/` | —      | done   |
| 2026-09-21 | 0.0g | Синхронизация плана с архитектурой: глобальный `RepositoriesModule` (1.2) и `BoardAccessModule` (2.1), регистрация репозиториев по шагам, Redis Pub/Sub в 2.6 (+ зависимости `0.3`/`2.5`), ретрофит-зависимости 2.6/3.1, `test:e2e` в командах проверки, `db:*` перенесены в 1.2, дедуп harness 6.1 | grep-чеклист + `git diff docs/` | —      | done   |
| 2026-09-21 | 0.1  | Монорепо: pnpm workspaces + Turbo, ESLint/Prettier, commitlint + husky (lint-staged), tsconfig.base, `.env.example`, `.gitignore` | `pnpm lint && pnpm typecheck && pnpm format:check`; коммит с плохим msg отклонён | 34273ea | done   |
| 2026-09-21 | 0.2  | Shared-пакет `@min-trello/shared`: zod-схемы (auth/user/board/column/card/label/comment/activity/common), типы через `z.infer`, env-схемы + `parseServerEnv`/`parseClientEnv`, константы (Priority, лимиты, ErrorCode, actions), dual ESM/CJS (tsup), Vitest — 23 теста. Подключение к apps отложено до 1.1/5.1 (приложений ещё нет) | `pnpm --filter @min-trello/shared build && pnpm --filter @min-trello/shared test`; `parseServerEnv({})` бросает с перечнем полей | 1474da9 | done   |
| 2026-09-21 | 0.3  | `docker-compose.dev.yml`: postgres:16 + redis:7, volumes, healthchecks; `.env` с `DATABASE_URL`/`REDIS_URL` (dev-Postgres на хосте 5433, т.к. 5432 занят системным postgresql@12) | `docker compose -f docker-compose.dev.yml up -d`; `ps` → оба healthy; `pg_isready` → accepting; `redis-cli ping` → `PONG` | 31f6e96 | done   |
| 2026-09-21 | 1.1  | Bootstrap NestJS: `main`/`app.module`, env-валидация через `parseServerEnv` (+`dotenv` для корневого `.env`), глобальные `ZodValidationPipe` (nestjs-zod), `AllExceptionsFilter` (`ApiError`), `LoggingInterceptor` (winston, JSON), helmet/CORS/throttler, Swagger `/api/docs`, health `/api/health` | `pnpm --filter @min-trello/backend test`; `curl /api/health` → `{status:'ok'}`; `/api/docs` → 200; unknown → 404 `NOT_FOUND` | 3d9dbf8 | done   |
| 2026-09-21 | 1.2  | Prisma: схема по `05-database.md` (9 моделей, enum `Priority`, индексы, каскады), миграция `init`, `PrismaService`/`PrismaModule` (`@Global`), пустой `@Global RepositoriesModule`, интерфейсы+токены репозиториев (`*/repositories/*.repository.ts`), `PrismaHealthIndicator`, root `db:*`-скрипты, e2e-harness на `DATABASE_URL_TEST` (миграции в `globalSetup`, truncate между тестами) | `pnpm db:deploy`; `curl /api/health` → `info.db.status=up`; `pnpm --filter @min-trello/backend test` (unit, без БД); `... test:e2e` | 1cd3bdf | done   |
| 2026-09-22 | 1.3  | Auth (JWT + refresh): `register`/`login`/`refresh`(ротация + grace)/`logout`/`me`; bcryptjs (`BCRYPT_ROUNDS=10`), SHA-256 refresh в `RefreshToken`; httpOnly/SameSite=Lax/`Path=/api/auth` cookie; глобальный `JwtAuthGuard` + `@Public()`/`@CurrentUser()`, `LocalStrategy`/`JwtStrategy`; `PrismaUserRepository` + `PrismaRefreshTokenRepository` в `RepositoriesModule` (user-репо вынесен из 1.4), `logout` публичный (по refresh-cookie, удаляет запись) | `pnpm --filter @min-trello/backend test` (unit) и `test:e2e`; curl-цикл register→refresh→me→logout→refresh(`401`) | b19e972 | done   |
| 2026-09-22 | 1.4  | Users: `GET /api/users` (массив из текущего пользователя, ownership), `PATCH /api/users/me` (смена `name`), `PATCH /api/users/me/password` (проверка старого → bcryptjs-хеш нового → hard-revoke всех refresh); `UsersModule`/`UsersService`/`UsersController` + zod-DTO; `revokeAllForUser` = удаление записей, ответы без `password` | `pnpm --filter @min-trello/backend test` и `test:e2e` (401 без токена, нет `password`, смена имени, старый refresh после смены пароля → `401`) | 8140740 | done   |
| 2026-09-23 | 2.1  | Boards CRUD + ownership: `BoardsController` (list/create/get/patch/delete), `BoardsService` на `BOARD_REPOSITORY_TOKEN` → `PrismaBoardRepository` (findById / findByIdWithColumns c columns+cards / findByOwner с пагинацией и `search`), `BoardAccessResolver` (метод + шаблон маршрута) и `BoardAccessGuard` (ownerId ≠ user.id → `404 BOARD_NOT_FOUND`) в `@Global BoardAccessModule`, `boardListQuerySchema` в shared | `pnpm --filter @min-trello/backend test` (6 suites/40) и `test:e2e` (4 suites/18); курлы из плана | 7042cef | done   |
| 2026-09-23 | 2.2  | Columns CRUD + порядок: `ColumnsController` (create в доске, patch/delete), `ColumnsService` на `COLUMN_REPOSITORY_TOKEN` → `PrismaColumnRepository` (create с `order=max+1` в транзакции и retry на `P2002`; реордер — двухфазная перенормировка `0..n-1` через `placeAtEnd`), guard через resolver (`/boards/:boardId/columns` и `column→board` для `/columns/:id`), `COLUMN_REPOSITORY_TOKEN` в `RepositoriesModule`, unit (`placeAtEnd`, `ColumnsService`) и e2e (порядок 0,1,2, реордер, чужой board → 404) | `pnpm --filter @min-trello/backend test` (8 suites/51) и `test:e2e` (5 suites/26); курлы из плана | 8d8b973 | done   |
| 2026-09-23 | 2.3  | Cards CRUD + reorder + assignee: `CardsController` (create в колонке; get/patch/move/assignee/delete), `CardsService` на `CARD_REPOSITORY_TOKEN` → `PrismaCardRepository` (create с `order=max+1`; `move` — транзакционная двухфазная перенормировка обеих колонок через `planCardMove`; `update` с `expectedUpdatedAt` → атомарный `updateMany` + `409`; assignee = владелец доски, иначе `422`), guard через resolver (`/columns/:columnId/cards` и `card→column→board`), `CurrentBoard`-декоратор, `CARD_REPOSITORY_TOKEN` в `RepositoriesModule`, `placeAtEnd` вынесен в `common/order.util`, unit (`planCardMove`, `CardsService`) и e2e (порядок, межколоночный move, `409`, assignee `422`, чужой board → `404`) | `pnpm --filter @min-trello/backend test` (10 suites/73) и `test:e2e` (6 suites/39); курлы из плана | 87646c2 | done   |
| 2026-09-23 | 2.4  | Labels board-scoped: `LabelsController` (create/list в доске, delete, attach/detach карточки), `LabelsService` на `LABEL_REPOSITORY_TOKEN` → `PrismaLabelRepository` (уникальность `[boardId,name]` → `409`, проверка `label.boardId === card.column.boardId` → `422 INVALID_LABEL`), resolver: `/boards/:boardId/labels`, `/labels/:id`, `/cards/:cardId/labels[/:labelId]`, `LABEL_REPOSITORY_TOKEN` в `RepositoriesModule`, `addLabelSchema` в shared, unit (`LabelsService`) и e2e (уникальность, привязка/отвязка, чужой board → `404`, чужая метка → `422`) | `pnpm --filter @min-trello/backend test` (11 suites/84) и `test:e2e` (7 suites/46); курлы из плана | 8025d7a | done   |
| 2026-09-23 | 2.5  | Comments: `CommentsController` (список комментариев карточки, создание, удаление), `CommentsService` на `COMMENT_REPOSITORY_TOKEN` → `PrismaCommentRepository` (автор через `include`, пагинация `createdAt desc`, удаление только автором → `403`, отсутствующий → `404 COMMENT_NOT_FOUND`), resolver: `/cards/:cardId/comments` (`card→column→board`) и `/comments/:id` (`comment→card→board`), `COMMENT_REPOSITORY_TOKEN` в `RepositoriesModule`, `CommentsModule` в `AppModule`, unit (`CommentsService`) и e2e (создание с `authorId`/`createdAt`, пагинация, удаление, чужой board → `404`) | `pnpm --filter @min-trello/backend test` (12 suites/91) и `test:e2e` (8 suites/53); курлы из плана | db5b5ff | done   |
| 2026-09-23 | 2.6  | Activity + SSE: `ActivityModule` (`ActivityController`: список + `@Sse`-поток, `ActivityService` на `ACTIVITY_REPOSITORY_TOKEN` → `PrismaActivityRepository`), `RedisModule`/`RedisService` (publisher + subscriber), `log(boardId, action, payload, actorId, cardId?)` пишет в БД и публикует в `board:{boardId}:activity`; подписка `board:*:activity` и fan-out локальным SSE-подписчикам (без локальной эмиссии), heartbeat `ping` каждые 15 c; ретрофит `BoardsService`/`CardsService`/`CommentsService` (board/card/comment/assignee), resolver: `/boards/:id/activity[/stream]`; unit (`ActivityService`) и e2e (REST + SSE activity/ping, чужой board → 404) | `pnpm --filter @min-trello/backend test` (13 suites/96) и `test:e2e` (9 suites/57); `curl -N .../activity/stream` | b42b8e3 | done   |
| 2026-09-23 | 2.7  | Search + Dashboard: `SearchModule` (`SearchController`: board-scoped `/boards/:id/search` и глобальный `/search`, `SearchService` на `SEARCH_REPOSITORY_TOKEN` → `PrismaSearchRepository` через `buildCardSearchWhere` — owner-scope, `q`/priority/label/assignee/hasDeadline), `DashboardModule` (`DashboardController`: `/dashboard/stats`, `DashboardService` на `DASHBOARD_REPOSITORY_TOKEN` → `PrismaDashboardRepository`: totalBoards/totalCards/cardsByStatus/overdueCards вне `isDone`), `searchQuerySchema` в shared, resolver `/boards/:id/search`, токены в `RepositoriesModule`, unit (`buildCardSearchWhere`, `SearchService`, `DashboardService`) и e2e (фильтры, пагинация, scope, stats) | `pnpm --filter @min-trello/backend test` (16 suites/103) и `test:e2e` (11 suites/65); курлы из плана | 6d669a3 | done   |
| 2026-09-23 | 3.1  | Realtime: `BoardsGateway`/`CardsGateway` (default namespace, комната `board:{boardId}`), `WsAuthMiddleware` (JWT из `handshake.auth.token` → `socket.data.user`, применение через `WeakSet` без дубля), `RealtimeModule` (`@Global` JwtModule + middleware), `RedisIoAdapter` (`@socket.io/redis-adapter` + отдельные ioredis-соединения, CORS) в `main.ts`; `joinBoard`/`leaveBoard` с owner-check → `error { code: 'FORBIDDEN' }` без join; эмиссия `board.updated`/`column.*`/`card.*`/`comment.created` с `actorId`+`clientId` (ретрофит `BoardsService`/`ColumnsService`/`CardsService`/`CommentsService`), `@ClientId()` читает `X-Client-Id`; unit (gateways, middleware) и e2e (2 клиента — `card.moved`, без/битый токен → `connect_error`, чужой board → `FORBIDDEN`) | `pnpm --filter @min-trello/backend test` (19 suites/119) и `test:e2e` (12 suites/69); `curl /api/health` | 28a3c53 | done   |
| 2026-09-23 | 4.1  | Seed: `prisma/seed.ts` (идемпотентный `upsert`, детерм. id; 2 юзера `alice`/`bob` с `password123` (bcryptjs, раунды из `auth.constants`), 6 досок × 3 колонки (Done `isDone`), 120 карточек (60/юзер: все приоритеты, дедлайны вкл. просроченные, метки, assignee=владелец, 1–3 комментария), `createdAt/updatedAt` в прошлом, стабильный PRNG; `prisma/seed.check.ts` smoke; скрипты `db:seed`/`db:reset --force`/`test:seed` в корне и backend | `pnpm db:reset && pnpm db:seed && pnpm test:seed`; повторный seed идемпотентен | 7035ab8 | done   |
| 2026-09-23 | 5.1  | Frontend skeleton: `@min-trello/frontend` (Vite 8 + React 19 + TS, alias `@`, `@min-trello/shared` из dist), `app/{App,providers,routes,theme-provider}`, `shared/api/ky-client` (`prefix`, `credentials:'include'`, access-токен в памяти, `X-Client-Id` на мутациях, на 401 → single-flight refresh через `navigator.locks`+`BroadcastChannel` → повтор один раз, при провале refresh — `onUnauthorized`/logout), `shared/config/env` (`parseClientEnv`), `shared/api/{token-store,client-id,refresh}`, `features/auth` (zustand: login/register/logout/bootstrap, `useAuth`/`useAuthBootstrap`), `ProtectedRoute`, страницы-заглушки login/register/dashboard/404; Vitest 9 тестов (ky-интерцептор refresh+retry+X-Client-Id; auth-store login/logout/bootstrap) | `pnpm --filter @min-trello/frontend test`; `pnpm --filter @min-trello/frontend dev` → `/` редиректит на `/login`, после логина сессия переживает F5 | be1ee86 | done   |
| 2026-09-23 | 5.2  | UI kit и layout: Tailwind v4 (`@tailwindcss/vite`, токены light/dark в `globals.css`), shadcn/ui-компоненты в `shared/ui` (`Button`, `Input`, `Dialog`, `DropdownMenu`, `Badge`, `Card`, `Tooltip` + `index.ts`), `cn()`, общие состояния `Loading`/`EmptyState`/`ErrorBoundary`, страница 404, layout `app/layout` (Header с пользователем, выходом и переключателем темы + контент-область), Storybook 10 (`@storybook/react-vite` + docs/themes/a11y) и 3 stories (Button/Badge/Dialog), скрипты `storybook`/`build-storybook` | `pnpm --filter @min-trello/frontend test`; `pnpm storybook` → открывается; `pnpm --filter @min-trello/frontend build`; `pnpm build-storybook` | 3b261fe | done   |
| 2026-09-23 | 5.3  | Auth-экраны: `react-hook-form` + `@hookform/resolvers` (zodResolver) на схемах `loginSchema`/`registerSchema` из shared, `shared/ui/form`+`label` (shadcn `FormField`/`FormControl`/`FormMessage`), локализованная zod-карта ошибок (`ruZodErrorMap`) и разбор API-ошибок (401/409/422) с маппингом `EMAIL_TAKEN→email`, `register` гидратит сессию через `/auth/me`, `AuthLayout` для `/login`/`/register`, редирект на `/dashboard` (или исходный `from`); Vitest 23 теста (store register + страницы: валидация, сабмит, серверные ошибки) | `pnpm --filter @min-trello/frontend test`; `pnpm --filter @min-trello/frontend build`; вручную: регистрация → вход → `/dashboard` | cd36cda | done |
| 2026-09-23 | 5.4  | Dashboard + графики: `dashboardStatsSchema`/`DashboardStats` (+`CardsByStatus`) в shared, `PrismaDashboardRepository` переведён на shared-тип; `features/boards` — `api` (ky + zod `paginated(boardSchema)`), `useBoardsQuery` (react-query, `keepPreviousData`, пагинация `page/limit`) и `useCreateBoard` (инвалидация списка), `BoardList` с пагинацией и `CreateBoardDialog` (RHF+zod, `createBoardSchema`, маппинг серверных ошибок); `widgets/stats-chart` — `useDashboardStats`, карточки тоталов и Recharts `ResponsiveContainer` (пончик cardsByStatus с агрегацией по заголовку колонки + бар «Просрочено/Без просрочки»); `DashboardPage` собирает статистику и список досок; Vitest — парсинг/пагинация/создание/инвалидация, stats-хук (успех+невалидный payload), `aggregateByStatus` | `pnpm --filter @min-trello/frontend test`; `pnpm --filter @min-trello/frontend build`; вручную: логин seed-юзером → `/dashboard` (3 доски) и сверка графиков с `curl /api/dashboard/stats` | 9cac9f0 | done   |
| 2026-09-23 | 5.5  | Frontend Board view + DnD-каркас: `entities/board` (`fetchBoard`/`fetchBoards`/`createBoard`, `boardQueryKeys.detail`, `useBoardQuery`), `entities/column`/`entities/card` (`ColumnHeader`, `CardItem` с приоритетом/дедлайном), `widgets/board-column` (заголовок, счётчик, `isDone`, карточки по `order`, empty-state), `pages/board/BoardPage` (загрузка `/boards/:id`, loading/error/404/empty, `DragDropContext`-провайдер без move-логики), маршрут `/boards/:id`, board-API вынесен в entity и реэкспортирован из `features/boards`; зависимость `@hello-pangea/dnd` | `pnpm --filter @min-trello/frontend test` (15 файлов/49); `pnpm --filter @min-trello/frontend build`; вручную: доска из дашборда → `/boards/:id` | — | done   |
| 2026-09-23 | 5.6  | Card modal: `entities/card` (fetch detail/update/assignee/delete + `useCardQuery` и мутации с инвалидацией доски); `features/cards` (assignee из `/api/users`), `features/labels` (список/создание меток доски, attach/detach), `features/comments` (`useInfiniteQuery`-пагинация, создание, удаление только автором), `shared/ui/markdown` (`MarkdownRenderer` = react-markdown + remark-gfm + rehype-sanitize, без `rehype-raw`; `MarkdownEditor` = MDXEditor с live-превью), `widgets/card-modal` (CRUD полей с `expectedUpdatedAt`→`409`, метки, исполнитель, комментарии, удаление); маршрут `/boards/:id/cards/:cardId` (+`Outlet` в `BoardPage`), карточки-ссылки в колонках; Vitest — XSS-рендер, редактор (mock MDXEditor), card entity, card-modal (поля/метки/комментарии, save, close) | `pnpm --filter @min-trello/frontend test` (19 файлов/61); `pnpm --filter @min-trello/frontend build`; вручную: доска → карточка, изменить описание/метки/исполнителя/комментарий, F5 — сохранилось; XSS не исполняется | —      | done   |
| 2026-09-23 | 5.6a | Создание колонок/карточек в UI (доп. к 5.6): `entities/column` (`createColumn` + `useCreateColumn`), `entities/card` (`createCard` + `useCreateCard`), `features/cards/ui/AddCardForm` (инлайн-композер «+ Добавить карточку» в колонке), `features/boards/ui/AddColumnForm` («+ Добавить колонку» на доске), инвалидация `boardQueryKeys.detail`; тесты создания карточки/колонки | `pnpm --filter @min-trello/frontend test` (21 файл/65); `pnpm --filter @min-trello/frontend build`; вручную: «+ Добавить карточку»/«+ Добавить колонку» → появляются после рефетча доски | —      | done   |
| 2026-09-23 | 5.6b | Фикс: на пустой доске (0 колонок) не показывался композер «Добавить колонку» — строка колонок рендерилась только при `columns.length > 0`; теперь `DragDropContext` и `AddColumnForm` показываются всегда при загруженной доске, плюс подсказка «Колонок пока нет» | `pnpm --filter @min-trello/frontend test` (21/65); вручную: создать доску → «+ Добавить колонку» → в колонке «+ Добавить карточку» | —      | done   |
| 2026-09-23 | 5.6c | Счётчик карточек в списке досок: shared `boardListItemSchema` (`Board + cardsCount`), `GET /api/boards` отдаёт `Paginated<BoardListItem>` (`PrismaBoardRepository.findByOwner` суммирует `_count.cards` по колонкам), `BoardList` показывает бейдж с числом карточек рядом с названием; обновлён 06-api.md | `pnpm --filter @min-trello/backend test` (19/119) и `test:e2e boards.e2e-spec` (8); `pnpm --filter @min-trello/frontend test` (21/66); `pnpm build`; вручную: дашборд → у доски бейдж с числом карточек | —      | done   |
| 2026-09-23 | 5.7  | DnD + optimistic move: `entities/card/api` (`moveCard` → `PATCH /cards/:id/move`), `features/cards/model` (`applyCardMove` — чистая функция пересчёта `order` при переносе внутри/между колонками с реиндексацией 0..n-1; `useMoveCard` — react-query `onMutate` snapshot + `setQueryData`, rollback в `onError`, `invalidateQueries` board+card в `onSettled`), `BoardPage` `DragDropContext.onDragEnd` → `moveCard.mutate` (пропуск no-op), `BoardColumn` `Droppable`+`Draggable` (drag-handle на карточке, `placeholder`, empty-state как дроп-зона), баннер об откате при ошибке; Vitest — `applyCardMove` (внутри/между колонок, неизвестные id) и `useMoveCard` (оптимистичное состояние в полёте, rollback, invalidate) | `pnpm --filter @min-trello/frontend test` (23 файла/73); `pnpm --filter @min-trello/frontend build`; вручную: перетащить карточку, F5 — позиция сохранилась; ошибка → откат | —      | done   |
| 2026-09-23 | 5.7a | Фикс мигания карточек при днд: `useMoveCard.onMutate` сделан синхронным (snapshot + `setQueryData` до `cancelQueries`) — `@hello-pangea/dnd` требует применить новый порядок в том же тике, что и `onDragEnd`; `applyCardMove` сохраняет ссылки неизменённых колонок/карточек и реиндексирует только затронутые; `CardItem` и `BoardColumn` обёрнуты в `React.memo` | `pnpm --filter @min-trello/frontend test` (23/73); вручную: перетаскивание без мигания | —      | done   |
| 2026-09-23 | 5.7b | Фикс мигания «Нет карточек» после дропа: `notifyManager` из `@tanstack/query-core` переведён на `queueMicrotask` в `app/providers.tsx` — по умолчанию react-query шлёт уведомления обсерверам через `setTimeout(0)`, из-за чего оптимистичный порядок применялся уже после отрисовки старого состояния (в пустой колонке на кадр появлялась заглушка); микрозадача применяет кэш до paint, добавлена прямая зависимость `@tanstack/query-core` | `pnpm --filter @min-trello/frontend test` (23/73); вручную: перетаскивание без мигания «Нет карточек» | —      | done   |
| 2026-09-23 | 4.1a | Фикс seed↔контракт по id: seed отдавал `seed-…`-id, которые не проходили `idSchema = z.string().cuid()` (`common.ts`), из-за чего zod-парсинг ответов `/api/boards` и `/api/dashboard/stats` на фронте падал и дашборд показывал «Не удалось загрузить…» (баг видел любой seed-юзер). `seedId(scope)` = `c` + sha256-hex[0..24] — детерминированные id в cuid-формате, контракт и тесты не тронуты | `pnpm db:reset && pnpm db:seed && pnpm test:seed`; `curl`-логин alice → парсинг `paginated(boardListItemSchema)`/`dashboardStatsSchema`; в браузере дашборд alice показывает 3 доски по 20 карточек | —      | done   |
| 2026-09-23 | 5.8  | Фильтры и поиск: `features/filters` (`FilterValues`, `lib` — фильтры ↔ URL-query, `buildSearchQuery`, `hasActiveFilters`; Zustand `useFiltersStore`; `api` — `searchBoardCards`/`searchGlobalCards` (ky + zod `paginated(cardSchema)`); react-query `useBoardSearchQuery`/`useGlobalSearchQuery`; `useFiltersUrlSync` — двусторонняя синхронизация стора и URL), `shared/hooks/useDebouncedValue` (300 мс), `widgets/filter-bar` (текст + priority/label/assignee/deadline + сброс), `SearchResults` (пагинация; при `boardId` — ссылки на карточку), `pages/search/SearchPage`, ссылка «Поиск» в хедере; `BoardPage` — `FilterBar`, при активных фильтрах канбан заменяется результатами board-scoped поиска; закрытие карточки (`CardModal.close`) сохраняет query-строку с фильтрами | `pnpm --filter @min-trello/frontend test` (30 файлов/101); `build`; вручную: `/search?q=…` и фильтры на доске → те же данные, что `curl /api/search`; состояние в URL | —      | done   |
| 2026-09-23 | 5.9  | Activity (SSE) + live (Socket.IO): `shared/realtime` (`socket.ts` — singleton Socket.IO с `auth.token` из token-store, синхронизация токена через `subscribeAccessToken`, на `connect_error` single-flight refresh + reconnect; `sse.ts` — `openActivityStream` на `@microsoft/fetch-event-source` с Bearer, парсинг `activity`/`ping`, на ошибке refresh + reconnect), `entities/activity` (list API + zod `paginated(activityLogSchema)`, `useActivityQuery`), `features/live` (`events.ts` — список серверных событий, дедуп по `clientId` вкладки, маппинг событий → `invalidateQueries`; `useBoardLive` — `joinBoard`/`leaveBoard` с ре-join на `connect`; `useActivityLive` + `appendActivityToCache` — префикс activity в кэш без дублей, статус потока), `widgets/activity-log` (лента с человекочитаемыми действиями и индикатором SSE), встроено в `BoardPage`; `resetSocket` при logout/потере сессии; зависимости `socket.io-client`, `@microsoft/fetch-event-source`; Vitest +13 | `pnpm --filter @min-trello/frontend test` (34 файла/114); `build`; `lint`; smoke: WS-клиент — `joinBoard`+`card.moved` с `clientId`, битый токен → `connect_error UNAUTHORIZED`; `curl -N .../activity/stream` → `activity` при мутации | —      | done   |
| 2026-09-23 | 5.9a | Активность в выезжающей справа панели: `widgets/activity-log/activity-panel.tsx` (`ActivityPanel` — `transition-[width]` 0→`w-80`, `sticky`-список, `ActivityLog`/SSE монтируются только при `open`), кнопка-тоггл `PanelRightIcon` в хедере `BoardPage` (`aria-pressed`), контент доски в `flex-1 min-w-0` — панель отталкивает его, а не перекрывает | `pnpm --filter @min-trello/frontend test` (34/115); `build`; вручную: кнопка «Активность» → панель выезжает справа, контент сжимается | —      | done   |
| 2026-09-24 | 5.8a | Фикс сброса фильтров при открытии/закрытии карточки: ссылка на карточку в `BoardColumn` несла только путь (`cards/:cardId`), из-за чего при переходе в модалку терялась query-строка, а `useFiltersUrlSync` очищал стор фильтров; теперь ссылка дополняет путь `location.search` и сохраняет фильтры, и `CardModal.close` возвращает их в URL доски (`SearchResults` уже сохранял) | `pnpm --filter @min-trello/frontend test` (34/116); вручную: фильтры на доске → открыть карточку → закрыть → фильтры на месте | —      | done   |
| 2026-09-24 | 5.10 | Профиль: `features/auth` (`updateProfile` → `PATCH /api/users/me`, обновляет `user` в store; `changePassword` → `PATCH /api/users/me/password`), `pages/profile/ProfilePage` (две формы RHF+zod — имя и смена пароля; email read-only; уведомление об успехе; после смены пароля `logout` + редирект на `/login` с notice), ссылка «Профиль» в меню хедера, notice на `LoginPage`, маршрут `/profile`; Vitest — store `updateProfile`/`changePassword` + ProfilePage (сохранение имени, повторный вход, неверный старый пароль, валидация) | `pnpm --filter @min-trello/frontend test` (35/123); `build`; `lint`; вручную: `/profile` → сменить имя (обновилось в хедере) → смена пароля → повторный вход | —      | done   |
| 2026-09-24 | 5.11 | Адаптивность: `shared/hooks/use-media-query` (`useMediaQuery`/`useIsMobile`, SSR/jsdom-фолбэк `false`), `shared/ui/sheet` (`Sheet` на Radix Dialog, `side=bottom|right`, slide-анимации); `FilterBar` на мобиле — кнопка «Фильтры» + bottom-sheet со счётчиком активных (desktop — инлайн); `BoardPage` на мобиле — табы колонок (`tablist`/`tab`/`tabpanel`, одна активная колонка, `BoardColumn`/`AddColumnForm` получили `className`, `w-full` вместо `w-72`), активность — вкладка (на мобиле `ActivityLog` вместо боковой `ActivityPanel`, десктоп — панель справа); `CardModal` — полноэкранный sheet на `max-md` (модалка на `sm+`); DnD на тач — встроенный long-press touch-сенсор `@hello-pangea/dnd` (колонки на мобиле переключаются табами, горизонтальный DnD не используется); `StatsChart` — карточки `min-w-0`, `ResponsiveContainer` на всю ширину; Vitest +5 (`useMediaQuery`, табы/активность `BoardPage`, bottom-sheet `FilterBar`) | `pnpm --filter @min-trello/frontend test` (36 файлов/128); `lint`; `build`; вручную devtools 375px: login→dashboard→board→card→DnD без горизонтального скролла канбана | —      | done   |
| 2026-09-24 | 5.11a | Смена колонки из карточки (доп. к 5.11): `features/cards/ui/column-select` (`ColumnSelect` — селект колонок доски через `useBoardQuery` + `useMoveCard`, перенос в конец целевой колонки `order=cards.length`, индикатор ошибки перемещения), добавлен в `widgets/card-modal` (aside рядом с исполнителем) — позволяет сменить колонку без DnD, что закрывает мобильный режим, где колонки переключаются табами; Vitest — card-modal: смена колонки через селект (`PATCH /cards/:id/move`) | `pnpm --filter @min-trello/frontend test` (36 файлов/129); `lint`; `build`; вручную на мобиле: карточка → селект «Колонка» → другая колонка → F5 — сохранилось | —      | done   |
| 2026-09-24 | 6.1  | Backend integration/e2e harness: `test/utils/` — `app.ts` (`createTestApp` с `configureApp` и опцией `listen` для SSE/WS), `db.ts` (`truncateAllTables` всех таблиц кроме `_prisma_migrations`), `auth.ts` (`registerUser`/`loginAs`), `factories.ts` (board/column/card/label/comment); все 12 e2e-наборов переведены на общий harness (удалён `test/truncate.ts`, дубли `register`/`create*` убраны); в jest-конфиг добавлен `coverageThreshold` для доменных сервисов ≥80% (statements/branches/functions/lines), `activity.service` доведён до 100% бранчей; `test:e2e` идёт по `DATABASE_URL_TEST` через `--runInBand` (детерминизм на общей тестовой БД) | `pnpm --filter @min-trello/backend test` (19 suites/122); `pnpm --filter @min-trello/backend test:cov` (порог сервисов); `pnpm --filter @min-trello/backend test:e2e` (12 suites/70) | —      | done   |
| 2026-09-24 | 6.2  | Frontend unit + Storybook: зависимость `msw`; `test/msw/` (`data.ts` — детерм. фикстуры auth/boards/cards/activity + `resetDb`, `handlers.ts` — auth/users/boards/labels/activity/columns/cards, `server.ts`/`index.ts`), `test/setup.ts` — `server.listen({onUnhandledRequest:'error'})`/`resetHandlers`/`resetDb`/`close`; интеграционные тесты через реальный ky-клиент (`features/auth/store.msw.test.ts` — login/register/me/profile/logout, `test/msw/msw.test.ts` — boards/cards/activity/users, 401 без токена); Storybook: декораторы `test/storybook.tsx` (QueryClient/Router/DnD) и stories CardItem/ColumnHeader/EmptyState/Loading/MarkdownRenderer/StatsChart/BoardColumn | `pnpm --filter @min-trello/frontend test` (38 файлов/141); `lint`; `typecheck`; `build`; `build-storybook` | —      | done   |
| 2026-09-24 | 6.3  | Playwright E2E: `playwright.config.ts` (два `webServer` — backend на `:3001` с `DATABASE_URL_TEST` и frontend `vite --mode e2e` на `:5174`; `globalSetup` = `prisma migrate reset` тестовой БД) + `e2e/auth.setup.ts` (storageState alice); сценарии: регистрация/логин (+неверные креды), создание доски/колонки/карточки, DnD между колонками, фильтры (текст+приоритет), комментарий, live (2 контекста через websocket); `.env.e2e`; попутный фикс `PublicOnlyRoute` — форма логина больше не размонтируется при `status="loading"` (терялась серверная ошибка) | `pnpm e2e` (8 тестов/7 файлов); `pnpm e2e:report`; `pnpm --filter @min-trello/frontend test` (38/141) | —      | done   |
| 2026-09-24 | 7.1  | Dockerfiles + nginx + полный compose: `docker/backend.Dockerfile` (multi-stage `node:20-alpine`+openssl, `--filter` install, `prisma generate` c musl-`binaryTargets`, non-root, `HEALTHCHECK`, entrypoint `prisma migrate deploy && prisma db seed` -> `node dist/main.js`); `docker/frontend.Dockerfile` (Vite build c build-ARG `VITE_*` -> nginx со SPA `try_files`); `docker/nginx.conf` (edge: SPA + `/api` + `/socket.io` + SSE-location с `proxy_buffering off`); корневой `docker-compose.yml` (`name: min-trello`; nginx/frontend/backend/db/redis, healthchecks, `depends_on.condition`, volumes, container-дефолты + override через `MIN_TRELLO_*`); `.dockerignore`; docker-блок в `.env.example` | `docker compose up -d --build`; `/api/health` -> `ok`; `/` и `/boards/x` -> `200` (SPA deep-link); логин `alice@example.com` -> token; seed 2/6/18/120; WS `joinBoard`+`column.created` и SSE `ping` через edge | —      | done   |
| 2026-09-24 | 7.2  | CI/CD: `.github/workflows/ci.yml` — `quality` (install `--frozen-lockfile`, `db:generate`, `format:check`, `lint`, `typecheck`, `test`, `build`), `e2e` (сервисы postgres+redis, создание `min_trello_test`, `prisma migrate deploy`, backend `test:e2e`, кэш `~/.cache/ms-playwright` + `playwright install --with-deps chromium`, `pnpm e2e`, артефакт HTML-отчёта), `images` (buildx-сборка обоих Dockerfile с GHA-кэшем); `.github/workflows/deploy.yml` — на `master`/`main`/теги `v*` build+push образов backend/frontend в GHCR (`docker/metadata-action`, `latest` на дефолтной ветке), job `deploy` (`docker compose pull backend frontend` + `up -d` по SSH через `appleboy/ssh-action`, секреты `DEPLOY_*`/`GITHUB_TOKEN`); в `docker-compose.yml` добавлены `image:`-ссылки на GHCR (`MIN_TRELLO_BACKEND_IMAGE`/`MIN_TRELLO_FRONTEND_IMAGE`) — локальный `docker compose up` собирает, CD-хост делает `pull`; обновлён `.env.example` | `pnpm exec prettier --check .github/workflows/*.yml docker-compose.yml`; `docker compose config -q`; push/PR → Actions: `quality`+`e2e`+`images` зелёные, сломанный тест → красный | — | done |
| 2026-09-24 | 7.2a | Фикс CI (unit фронта падали в Actions): без корневого `.env` `VITE_API_URL` уходил в дефолт `/api`, и ky + node-интерцептор MSW не парсили относительный URL (`Invalid URL`). Добавлен корневой `.env.test` (mode `test`, `envDir` = корень) с абсолютными `VITE_API_URL`/`VITE_WS_URL`; в `deploy.yml` `secrets` в `if` недопустим → хост вынесен в job-`env` и проверяется `env.DEPLOY_HOST != ''`, а `build-and-push` защищён guard'ом на release-ref (`master`/`main`/теги) | симмуляция CI: `mv .env /tmp/opencode/env.bak && pnpm test && pnpm build`; локально `pnpm test`; push → Actions `quality` зелёный, deploy больше не стартует на feature-ветке | — | done |
| 2026-09-24 | 7.2b | CI на Node 22: `jsdom@30`/`undici@8` не стартуют на Node 20 (`TypeError: webidl.util.markAsUncloneable is not a function` → падали все 38 файлов unit-тестов фронта). `NODE_VERSION` в `ci.yml` поднят `20`→`22` (локальный dev — Node 26, проверено и на 22) | симуляция: `nvm use 20 && pnpm --filter @min-trello/frontend test` падает; `nvm use 22` → 141 passed; push → Actions зелёные | — | done |
| 2026-09-24 | 7.2c | Причина падения E2E в CI: Turbo 2 по умолчанию `envMode: "strict"` и не пробрасывал `DATABASE_URL_TEST`/`REDIS_URL`/`JWT_SECRET` в turbo-задачу `e2e` (локально маскировал корневой `.env`) — Playwright падал в `e2e/env.ts` до старта, поэтому не было отчёта. В `turbo.json` задачам `e2e` и `test:e2e` добавлен `passThroughEnv` с этими переменными | симуляция CI: `mv .env /tmp && DATABASE_URL_TEST=… REDIS_URL=… JWT_SECRET=… pnpm e2e` (8 passed) и `pnpm test:e2e` (12 suites/70); push → Actions зелёные | — | done |
